
<div class="container">
  <h1>Create your best cocktail</h1>
  <p>let's do it"</p>
</div>

<!-- qua devo fare form per creare cocktail  -->
  <div class="row">
    <div class="col-xs-10 col-xs-offset-1">
      <%= simple_form_for(@cocktail) do |f| %>
        <%= f.error_notification %>
        <%= f.input :name %>
        <%= f.input :photos, as: :attachinary %>
        <%= f.button :submit, class: "btn btn-primary" %>
      <% end %>
    </div>
  </div>
</div>


<%= link_to "Back to the Homepage", cocktails_path %>
